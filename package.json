{
  "name": "pong-wars",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": ["worker", "app"],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "preview": "npm run preview -w app",
    "fmt": "npm run fmt --workspaces",
    "lint": "npm run lint --workspaces",
    "check": "npm run check --workspaces"
  }
}
